import 'package:flutter/material.dart';

class DefaultHeightSizedBox extends StatelessWidget {
  const DefaultHeightSizedBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16.0,);
  }
}
